DECLARE PUBLIC STATIC CLASS GameConstants
    DECLARE PUBLIC CONSTANT STRING MAIN_MENU <- "main menu"
    DECLARE PUBLIC CONSTANT STRING OPTIONS_MENU <- "options menu"
    DECLARE PUBLIC CONSTANT STRING CREDITS_MENU <- "credits menu"
    DECLARE PUBLIC CONSTANT STRING LEVEL_SELECTION_MENU <- "level selection menu"
    DECLARE PUBLIC CONSTANT STRING LEVEL_ONE_PART_ONE <- "level one part one"
    DECLARE PUBLIC CONSTANT STRING LEVEL_ONE_PART_TWO <- "level one part two
    DECLARE PUBLIC CONSTANT STRING LEVEL_TWO_PART_ONE <- "level two part one"
    DECLARE PUBLIC CONSTANT STRING LEVEL_TWO_PART_TWO <- "level two part two"
    DECLARE PUBLIC CONSTANT VECTOR2 CUSTOMER_SPAWNING_LOCATION <- NEW VECTOR2(0.0f, 0.0f) // to be refined

    DECLARE PUBLIC CONSTANT STRING KITCHEN <- "kitchen"
    DECLARE PUBLIC CONSTANT STRING RESTAURANT <- "restaurant"

    DECLARE PUBLIC CONSTANT INT CUSTOMER_SPAWN_RATE_MIN <- 5 // seconds
    DECLARE PUBLIC CONSTANT INT CUSTOMER_SPAWN_RATE_MAX <- 15 // seconds

    DECLARE PUBLIC CONSTANT FLOAT PLAYER_MOVEMENT_SPEED <- 5; // will need to be refined

    DECLARE PUBLIC CONSTANT STRING SUSHI <- "sushi"
    DECLARE PUBLIC CONSTANT STRING RAMEN <- "ramen"
    ...

    DECLARE PUBLIC CONSTANT LIST OF STRINGS MEALS <- { SUSHI, RAMEN, ... }
    DECLARE PUBLIC CONSTANT INT NUMBER_OF_MEAL_OPTIONS <- 2

    DECLARE PUBLIC CONSTANT INT NUMBER_OF_TABLES <- 4 // can be refined
    DECLARE PUBLIC CONSTANT INT NUMBER_OF_SEATS_PER_TABLE <- 4

    DECLARE PUBLIC CONSTANT VECTOR2 SUSHI_APPLIANCE_LOCATION <- NEW VECTOR2(..., ...) // needs placement
    DECLARE PUBLIC CONSTANT VECTOR2 RAMEN_APPLIANCE_LOCATION <- NEW VECTOR2(..., ...)
    ... // repeat for others
    DECLARE PUBLIC CONSTANT VECTOR2 TRASH_APPLIANCE_LOCATION <- NEW VECTOR2(..., ...)

    DECLARE PUBLIC CONSTANT STRING SUCCESS <- "success"
    DECLARE PUBLIC CONSTANT STRING FAIL <- "fail"
END CLASS